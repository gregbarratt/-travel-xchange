# Supabase

Phase 3 uses the browser Supabase client for registration, login, onboarding,
the member dashboard, the Xchange Feed, post likes, comments, and follow data.

Use `.env.local` for real Supabase keys. Do not commit `.env.local`.
